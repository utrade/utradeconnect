# This is a sample .env file that contains placeholders for various API keys, access tokens, and base URLs.
# Fill in the appropriate values for each variable before using it in your application.
# Make sure to keep this file secure and do not share it with others.
```
Sample .env file
```
ORDER_API_KEY=
ORDER_API_SECRET=
MARKET_DATA_API_KEY=
MARKET_DATA_API_SECRET=
ACCESS_TOKEN=
BASE_URL=